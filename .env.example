# Secrets & Deployment Configuration
DB_PASS=your_secure_password_here
API_KEY=your-secure-api-key-here

# Environment-specific Configuration
DB_HOST=postgres  # Use 'localhost' for local dev, 'postgres' for docker
PORT=8080         # May vary per deployment environment
ENVIRONMENT=development

# Optional: JWT Authentication (leave empty to disable)
JWT_SECRET=

# Note: Other configuration moved to config/app.yaml
# - Database user, name, port, max_connections
# - Rate limiting settings  
# - Feature flags (debug logging, prometheus, etc.)
# - Monitoring configuration